# SIGLAT API Environment Configuration Template
# Copy this file to .env and update with your actual values

# External Database Configuration
# Update these values to point to your existing PostgreSQL instance
DB_HOST=your-external-postgres-host    # e.g., localhost, postgres.example.com, or IP address
DB_PORT=5432                           # PostgreSQL port (default: 5432)
DB_USER=your-postgres-username         # PostgreSQL username
DB_PASS=your-database-password         # PostgreSQL password
DB_DB=siglat_db                        # Database name (will be created if doesn't exist)

# JWT Configuration (IMPORTANT: Change these in production!)
JWT_SECRET=your-super-secret-jwt-key-here-change-this-in-production
JWT_ISSUER=craftmatrix.org
JWT_AUDIENCE=siglat-api

# API Configuration
ASPNETCORE_ENVIRONMENT=Development      # Development, Production
ASPNETCORE_URLS=http://localhost:5000   # API listening URL

# CORS Origins (comma-separated list of allowed origins)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:8080

# Production Notes:
# - For Docker deployments, use DB_HOST=host.docker.internal to connect to host PostgreSQL
# - For external PostgreSQL servers, use the actual hostname/IP
# - Ensure your PostgreSQL server allows connections from the API container/host
# - Consider using connection pooling for production deployments